# 上游与修改记录

- 仓库：https://github.com/warren-bank/Android-Mock-Location
- 分支：service
- 固定提交：`142eb1b0262a3a17a34bc8266ca306c317e853b0`
- 上游提交标题：`release: service/v02.05.03`
- 修改版本：`002.05.03-personal.3-19API`
- 修改日期：2026-09-07～2026-09-08
- 许可证：保留仓库原有 GPL-2.0，见 `LICENSE.txt`；原作者和引用项目声明保留在源码中。

## 本次修改

保留 Java、Gradle、多构建变体、原有固定定位、书签、地图位置入口和两点移动。
新增原生路线界面、WGS84 有序途经点、GPX 文件导入、按统一速度播放、暂停／继续、终点保持和本地草稿保存。
路线复用提取出的坐标插值函数，使用单调时钟计时。经度插值处理跨日界线；长距离极区路线仍是坐标线性插值，非完整测地线规划。

固定位置、两点移动的起终点和多途经点路线均可打开全屏地图选点。地图使用固定版本 `osmdroid 6.1.20` 和高德在线栅格瓦片，保留 API 19；显示有序点、直线、高德署名，支持撤销、清空、旋转恢复和取消。大陆地图显示使用 GCJ-02，选择结果仍统一返回 WGS84。单点和空白路线优先使用系统当前非模拟位置，失败时回退原有或已保存位置。瓦片只缓存实际浏览区域，不进行道路规划、搜索或预下载。

服务改为 START_NOT_STICKY，补充销毁清理、通知停止、运行状态、坐标与模拟授权检查、定位失败提示。HMS 注入使用当前时间，不再沿用上游的 45 秒时间提前量。
HMS 变体移除 SDK 合并进来的安装其他应用及修改 Wi-Fi 状态权限，Core 更新由用户在应用外完成。
默认关闭悬浮摇杆，位置刷新默认一秒。路线播放固定一秒刷新，不受原有设置改变影响。
构建仓库由 JCenter 改为 Maven Central，独立安装包名为 `com.github.warren_bank.mock_location.personal`，最低 API 19。增加中文资源到调试版，并为在线地图增加网络权限。
修复上游 OsmAnd 分享链接缺少 `/` 的路径声明。模拟定位权限声明保留，用于开发者选项识别；仅对这条预期声明抑制 ProtectedPermissions 静态提示。

提供已签名的个人调试 APK。调试签名用于自用安装，源码重建时使用构建机器的调试密钥，因此不保证跨机器安装包的签名字节相同。发布到商店前需要自行管理正式签名。

## 2026-09-08 优化

坐标近似比较支持跨 ±180° 经线；坐标文本输出改用直接拼接，避免每个点创建格式化器。路线跨多段直接到终点时同步更新方向所属路段。
路线输入框使用固定资源 ID，支持系统恢复输入和光标；权限申请中的待启动路线随页面状态保存。地图开始手动点选、撤销或清空后取消初始定位，忽略晚到的定位结果。
构建脚本在复制 APK 后刷新 SHA-256 校验值。

## 清爽浅色 UI

统一浅灰背景、白色卡片、蓝色主操作和红色停止／删除样式，覆盖固定位置、两点移动、路线、地图、书签及设置。保留原生控件与原有导航，不增加依赖。
路线与地图静态布局迁入 XML；共享资源提供按钮状态、输入焦点、中文和默认文案以及不同 Android 版本的系统栏颜色。输入框 ID 和数据接口保持不变。
设备测试增加可选 UI 回归入口，覆盖书签、设置、GPX 回调和地图回填。详细记录见 `docs/UI-VERIFICATION.md`。
